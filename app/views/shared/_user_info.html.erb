<%= link_to gravatar_for(current_user, size: 50), current_user %>
<h3><%= current_user.name %></h3>
<span><%= pluralize(current_user.deals.count, "deals") %></span>
<span><%= link_to "Ir a Mi Perfil", current_user %></span>
